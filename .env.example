# Optional runtime overrides (not required for conversion)
DEFAULT_DOCX_TEMPLATE=template/default.docx

# Optional cloud layout analysis — auto-detected from process environment when set.
# No .env file is required; export these vars in your shell or CI if you use a cloud provider.
# DOCUMENT_LAYOUT_ENDPOINT=https://your-service.example.com/
# DOCUMENT_LAYOUT_API_KEY=your-key
